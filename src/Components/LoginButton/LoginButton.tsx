import styles from './LoginButton.module.css';

const LoginButton = () => {
    return (
        <div className={styles.button}>
            <p>Login</p>
        </div>
    );
}

export default LoginButton;
